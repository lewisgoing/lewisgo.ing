---
title: 'Projects'
description: 'A showcase of my web and audio development projects'
date: '2023-02-14'
tags: ['programming', 'algorithm', 'graph-theory']
author: 'lewisgoing'
technologies: ['Python', 'Number Theory', 'Cryptography', 'CTF']
thumbnailUrl: '/projects/project-1.png'
---



# Projects

I'm a full-stack developer with a passion for audio applications and music production. Here's a collection of my recent projects.

## Featured Projects

<div className="grid grid-cols-1 md:grid-cols-2 gap-6 my-8">
  <ProjectCard
    title="Harmony Hub"
    description="A specialized audio platform with an interactive EQ interface and real-time frequency visualization using Web Audio API."
    image="/images/projects/harmony-hub.jpg"
    technologies={['Next.js', 'TypeScript', 'Web Audio API', 'Tailwind CSS', 'Firebase']}
    githubUrl="https://github.com/lewisgoing/harmony-hub"
    liveUrl="https://harmony-hub.vercel.app"
    featured={true}
  >
    <ProjectMetrics 
      stars={42}
      forks={7}
      lastUpdated="March 2024"
    />
  </ProjectCard>
  
  <ProjectCard
    title="Personal Website"
    description="A modern portfolio website with an interactive 'bento grid' layout with draggable components and real-time integrations."
    image="/images/projects/personal-website.jpg"
    technologies={['Next.js 15', 'TypeScript', 'Tailwind CSS', 'Vercel']}
    githubUrl="https://github.com/lewisgoing/website"
    liveUrl="https://lewisgo.ing"
  >
    <ProjectMetrics 
      stars={21}
      forks={3}
      lastUpdated="March 2024"
    />
  </ProjectCard>
</div>

## Web Audio Explorations

I'm passionate about creating unique audio experiences on the web. Here's a demo of the Web Audio API in action:

<WebAudioDemo 
  title="Interactive Synthesizer"
  description="Try adjusting the frequency and waveform to create different sounds. This demonstrates the core functionality behind many of my audio applications."
/>

Here's a code sample showing how I implement audio visualizations:

<CodeBlock
  language="typescript"
  title="audioVisualizer.ts"
  showLineNumbers={true}
>
{`import { useEffect, useRef } from 'react';
export function useAudioVisualizer(audioElement: HTMLAudioElement | null) {
  const canvasRef = useRef<HTMLCanvasElement | null>(null);
  const analyserRef = useRef<AnalyserNode | null>(null);
  const animationRef = useRef<number | null>(null);
  
  useEffect(() => {
    if (!audioElement || !canvasRef.current) return;
    
    const audioContext = new AudioContext();
    const source = audioContext.createMediaElementSource(audioElement);
    const analyser = audioContext.createAnalyser();
    
    analyser.fftSize = 256;
    source.connect(analyser);
    analyser.connect(audioContext.destination);
    
    analyserRef.current = analyser;
    
    const bufferLength = analyser.frequencyBinCount;
    const dataArray = new Uint8Array(bufferLength);
    
    const canvas = canvasRef.current;
    const ctx = canvas.getContext('2d');
    const WIDTH = canvas.width;
    const HEIGHT = canvas.height;
    
    const renderFrame = () => {
      animationRef.current = requestAnimationFrame(renderFrame);
      
      analyser.getByteFrequencyData(dataArray);
      
      ctx.fillStyle = '#000';
      ctx.fillRect(0, 0, WIDTH, HEIGHT);
      
      const barWidth = (WIDTH / bufferLength) * 2.5;
      let x = 0;
      
      for (let i = 0; i < bufferLength; i++) {
        const barHeight = dataArray[i] / 255 * HEIGHT;
        
        const r = 166 - (i * (166 / bufferLength));
        const g = 79 + (i * (79 / bufferLength));
        const b = 225;
        
        ctx.fillStyle = \`rgb(\${r},\${g},\${b})\`;
        ctx.fillRect(x, HEIGHT - barHeight, barWidth, barHeight);
        
        x += barWidth + 1;
      }
    };
    
    renderFrame();
    
    return () => {
      if (animationRef.current) {
        cancelAnimationFrame(animationRef.current);
      }
      if (audioContext.state !== 'closed') {
        audioContext.close();
      }
    };
  }, [audioElement]);
  
  return canvasRef;
}
`}
</CodeBlock>

## Music Production

Listen to some of my recent productions:

<AudioPlayer
  src="/audio/closer.mp3"
  title="Closer Remix"
  showWaveform={true}
/>

<AudioPlayer
  src="/audio/winter22.mp3"
  title="Winter 22"
  showWaveform={true}
  accentColor="#10b981"
/>

## Research Publications

<ResearchPublication
  title="Exploring the Collaborative Co-Creation Process with AI: A Case Study in Novice Music Production"
  authors={["Yue Fu", "Michele Newman", "Lewis Going", "Qiuzi Feng", "Jin Ha Lee"]}
  publication="ACM Conference on Human Factors in Computing Systems (CHI)"
  date="2025"
  abstract="Artificial intelligence is reshaping creative domains, yet its co-creative processes, especially in group settings with novice users, remain under explored. Our findings highlight how AI transforms creative workflows: accelerating ideation but compressing the traditional preparation stage, and requiring novices to navigate a challenging idea selection and validation phase."
  pdfUrl="/publications/co-creation-ai.pdf"
  doiUrl="https://doi.org/10.1145/example"
  showAbstract={true}
/>

## Technologies I Use

<TechStack
  title="Frontend Technologies"
  technologies={[
    { name: "React", icon: "/icons/react.svg", url: "https://reactjs.org" },
    { name: "Next.js", icon: "/icons/nextjs.svg", url: "https://nextjs.org" },
    { name: "TypeScript", icon: "/icons/typescript.svg", url: "https://www.typescriptlang.org" },
    { name: "Tailwind CSS", icon: "/icons/tailwind.svg", url: "https://tailwindcss.com" },
    { name: "Web Audio API", icon: "/icons/webaudio.svg", url: "https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API" }
  ]}
/>

<TechStack
  title="Backend & DevOps"
  technologies={[
    { name: "Node.js", icon: "/icons/nodejs.svg", url: "https://nodejs.org" },
    { name: "Firebase", icon: "/icons/firebase.svg", url: "https://firebase.google.com" },
    { name: "Supabase", icon: "/icons/supabase.svg", url: "https://supabase.io" },
    { name: "Vercel", icon: "/icons/vercel.svg", url: "https://vercel.com" },
    { name: "GitHub", icon: "/icons/github.svg", url: "https://github.com" }
  ]}
/>